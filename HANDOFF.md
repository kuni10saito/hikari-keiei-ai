# 引き継ぎ資料 — 経営演習AI

別の環境・別のアカウントの Claude Code でこのプロジェクトを引き継ぐための資料。
**まずこのファイルを最初から最後まで読むこと。** 特に「ハマりどころ」は
すべて実際に踏んだもので、読まずに進めると同じ失敗を繰り返す。

---

## 1. これは何か

名古屋学院大学 経営学部の演習用 Web アプリ。**学生がブラウザから Claude を使い、
実在企業の経営分析を行い、Excel・PowerPoint・Word・PDF の成果物を生成する。**

- **学生に Claude アカウントは不要。** API 課金は教員のキー1本に集約される
- 受講者は2名（課題として自宅から利用）
- 稼働中: https://hikari-keiei-ai.onrender.com/
- リポジトリ: https://github.com/kuni10saito/hikari-keiei-ai （**非公開**）

```
学生 → ブラウザ → FastAPI(Render) → Claude API（教員のキー）
                       ↓
              /var/data/outputs/<学籍番号>/*.xlsx をダウンロード提供
```

---

## 2. ファイル構成

```
company-ai/
├── app.py                      FastAPI 本体（すべてのロジック）
├── db.py                       SQLite（使用量・会話履歴・生成物記録）
├── static/index.html           画面（ダッシュボード＋チャット、依存ライブラリなし）
├── render.yaml                 Render Blueprint。★会社の切り替えはここ
├── requirements.txt
├── start_public.ps1            ローカルから Cloudflare Tunnel で公開する用
├── roster.example.txt          名簿の見本（実物は .gitignore）
│
├── company.md            + company_data.json            株式会社ヒカリ
├── company_pacolabo.md   + company_pacolabo_data.json   パコラボ株式会社
├── company_atsuta.md     + company_atsuta_data.json     あつた鶏飯 Re-Name Project
│
└── agents/atsuta/*.md          部門AI（サブエージェント）10部門
```

**リポジトリに入っていないもの**（.gitignore 済み・意図的）

| | 理由 |
|---|---|
| `roster.txt` | 学籍番号と実名。個人情報 |
| `usage.sqlite3` / `usage_backup_*.sqlite3` | 使用量と会話履歴 |
| `outputs/` | 学生の生成物 |
| `ANTHROPIC_API_KEY` | 環境変数のみ |

---

## 3. 動かす

### ローカル

```powershell
cd C:\Users\saito\Downloads\company-ai
$env:ANTHROPIC_API_KEY = "sk-ant-..."
$env:CLASS_PASSWORD    = "hikari"
python -m uvicorn app:app --host 127.0.0.1 --port 8000
```

> **`uvicorn` 単体では動かない。必ず `python -m uvicorn`。**
> この PC ではパッケージが `--user` 側（`AppData\Roaming\Python\Python314\Scripts\`）に
> 入っており、そのフォルダが PATH に無いため `uvicorn.exe` が見つからない。

起動バナーで設定を確認できる。**`データ保存先` の行が最重要**（後述）。

### Render（本番）

Blueprint 管理。`render.yaml` を push すれば環境変数も同期される。
秘密情報（`sync: false`）だけはダッシュボードで入力済み。

| 変数 | 内容 |
|---|---|
| `ANTHROPIC_API_KEY` | ダッシュボードで設定済み |
| `CLASS_PASSWORD` | 学生に配る合言葉 |
| `ADMIN_KEY` | 教員用API の鍵 |
| `ROSTER` | `28b0113` （**2人目が未追加**） |
| `COMPANY_FILE` | `render.yaml` の45行目 |

費用: Starter $7/月 ＋ ディスク $0.25/月 ≒ **1,130円/月**。
**課題期間が終わったらサービスを削除しないと課金され続ける。**

---

## 4. 教員用API

`?key=<ADMIN_KEY>` が必要。`<>` は付けず値だけ書く。

| URL | 用途 |
|---|---|
| `/api/admin/status?key=...` | 設定と現状。**`disk_ok` が最重要** |
| `/api/admin/usage?key=...` | 学生ごとの累計＋費用の内訳 |
| `/api/admin/turns?key=...` | **1ターンずつの明細**。どの依頼が高かったか特定できる |
| `/api/admin/reset?key=...&student_id=28b0113` | その学生の使用量・履歴・生成物記録を消す |

---

## 5. 会社の切り替え

`render.yaml` の45行目を書き換えて push するだけ。

```yaml
      - key: COMPANY_FILE
        value: company_atsuta.md
```

| 値 | 会社 | 財務の性質 | 表示単位 |
|---|---|---|---|
| `company.md` | 株式会社ヒカリ | **架空**（演習用に作成）・3期 | 百万円 |
| `company_pacolabo.md` | パコラボ株式会社 | 実データ（第10期決算） | 千円 |
| `company_atsuta.md` | あつた鶏飯 Re-Name Project | 実データ（2026年5月） | 円 |

対になる `_data.json` は**ファイル名の規則で自動選択**される
（`company_X.md` → `company_X_data.json`）。

**切り替えたら会話履歴をリセットすること。** 前の会社の会話が残ったまま
新しい会社の設定で応答してしまう。

### 会社を追加する手順

1. `company_<key>.md` — AIが読む会社設定（system プロンプト）
2. `company_<key>_data.json` — ダッシュボードの数値
3. 必要なら `agents/<key>/*.md` — 部門AI
4. `render.yaml` の `COMPANY_FILE` を変えて push

`_data.json` のスキーマは既存3社を見れば分かる。**欠けている項目のカードは
自動的に非表示になる**ので、揃わなくてよい。

| データ | 無いとどうなる |
|---|---|
| `periods` が1件 | 推移チャートを出さない |
| `segments[].gross_profit` | セグメント粗利率チャートを出さない |
| `bs` が null | 自己資本比率KPIを出さない |
| `cf` が null | 営業CF の代わりに当期純利益を出す |
| `sga` | 販管費構成チャートを出さない |
| `extra_chart` | その会社固有のチャートを出さない |
| `custom_kpis` | 追加KPIを出さない |

---

## 6. ★ハマりどころ（すべて実際に踏んだ）

### 6-1. 永続ディスクが最重要

`DATA_DIR=/var/data` が効いていないと、**再デプロイのたびに `usage.sqlite3` が
消えて利用上限が0円にリセットされ、API課金の歯止めが完全に外れる。**

確認方法は `/api/admin/status` の `disk_ok`。`false` なら
Render の Settings → Disks で `/var/data` を追加する。

### 6-2. 費用の9割はキャッシュ書込

**実測値（claude-sonnet-5 / effort=medium）**

| 依頼 | 費用 | 内訳の特徴 |
|---|---:|---|
| 分析・議論 | 2〜5円 | |
| Excel 生成 | 約18円 | |
| **スライド生成** | **約156円** | **cache_write が92.5%** |

スライドが高いのは、Anthropic 側サーバ内部の code execution ループが
文脈を何度も書き直すため。**`task_budget` も `MAX_RESUME` も届かない領域。**
そのため `PPTX_LIMIT`（既定1回）で回数制限している。上限に達した学生には
`pptx` スキル自体を渡さないので、費用が発生する前に封じられる。

### 6-3. 会話履歴の刈り込みが必須

**context editing はサーバ側でモデルに見せる文脈を削るだけで、
こちらが送信する量は減らない。** ここを誤解して1度失敗した。

対策前はスライド1回で履歴が 59,439 → **536,373 文字**に膨れ、
以後毎ターン再送されていた。`_prune()` でテキスト以外のブロックを
落とすようにして **99.96% 削減**（550,498 → 217 文字）。

**この処理を外すと費用が雪だるま式に増える。**

### 6-4. プロンプトキャッシュに可変情報を混ぜない

`system` の先頭は `company_*.md` で固定。**ここに日時・学生名・可変情報を
入れるとキャッシュが毎回無効化され、部門AIの並列起動も高くなる。**
可変情報は必ず `messages` 側に置く。

### 6-5. 上限判定はターン開始前にしかできない

1ターンの途中では止められないので、残額わずかで重い依頼を始めると超過する
（実測で1ターン262円）。`TURN_RESERVE_YEN`（pptx可のとき170円、
使い切り後30円）で開始前に余力を要求している。

### 6-6. Windows PowerShell 5.1 の落とし穴

| 症状 | 原因 | 対処 |
|---|---|---|
| `.ps1` が構文エラー | **BOM なし UTF-8 を CP932 として読む** | ファイルに UTF-8 BOM を付ける |
| `NativeCommandError` が大量に出る | **native exe に `2>&1` を使った** | ログをファイルに逃がして読む |
| `uvicorn` が見つからない | PATH に無い | `python -m uvicorn` |

### 6-7. 日本語IMEの全角入力

学籍番号を全角（`２８ｂ０１１３`）で打つと一致しない。
`_norm()` で NFKC 正規化・小文字化・空白除去して吸収済み。

### 6-8. 個人情報とバックアップの流出

- `roster.txt` は学籍番号と実名を含むので**環境変数 `ROSTER` に移した**
- 自分で作った `usage_backup_*.sqlite3` が `.gitignore` から漏れて
  **コミットに含まれていた**（push 前に気づいて履歴ごと除去）。
  今は `*.sqlite3` も除外している

**`.gitignore` は「作るファイル名を全部予測して書く」のが難しい。
push 前に `git ls-files` で必ず目視確認すること。**

---

## 7. 部門AI（サブエージェント）

`agents/<会社キー>/<部門>.md` を置いた会社でのみ有効。現在はあつた鶏飯の10部門。

| 学生の入力 | 動作 | API呼び出し |
|---|---|---|
| `@` 無し | 従来どおり | 1回 |
| `@finance 原価率は？` | その部門の役割定義を system に足す | 1回 |
| `@finance @quality @legal 〜` | **並列起動 → CEOが統合** | 4回 |

- 上限は `MAX_AGENTS_PER_TURN`（既定4）
- **部門呼び出しはテキストのみ**（スキル・コード実行なし、`max_tokens=2000`、`effort=low`）
- **成果物を作れるのは統合側だけ。** 部門ごとに作らせると費用が部門数倍になる
  （3部門がそれぞれスライドを作ると約470円）
- 画面では各部門の見解を個別表示する。**どの部門が何と言ったかが見えることが演習の核心**

費用は3部門＋統合で約9円。部門呼び出しが同じ `company_*.md` を system の先頭に
置くため、2つ目以降はキャッシュ読出（0.1倍）で済む。

---

## 8. 利用上限

| 環境変数 | 既定 | 対象 |
|---|---:|---|
| `DAILY_YEN_CAP` | 300円 | 学生1人 / 1日 |
| `DAILY_TOTAL_YEN_CAP` | 600円 | クラス全体 / 1日 |
| `PERIOD_YEN_CAP` | 500円 | 学生1人 / 課題期間の通算 |
| `PERIOD_TOTAL_YEN_CAP` | 1,200円 | クラス全体 / 課題期間の通算 |
| `PPTX_LIMIT` | 1回 | 学生1人あたりのスライド作成 |

**日次だけでは歯止めにならない**（課題が4週間なら28回リセットされる）。
最終的に止めるのは通算。通算は `usage.sqlite3` 全件の集計なので、
**永続ディスクが効いていることが前提**（6-1参照）。

`MODEL` を変えるときは `PRICE_INTRO` / `PRICE_STANDARD` を**必ずセットで**直すこと。
単価がずれると上限が意図どおり効かなくなる。
なお claude-sonnet-5 は 2026-08-31 まで導入価格 $2/$10、以降 $3/$15 で、
日付で自動的に切り替わるようにしてある。

---

## 9. 実在企業データの扱い

| 会社 | 財務 | 注意 |
|---|---|---|
| ヒカリ | **架空**（演習用に作成） | 実在企業なので、第三者が実データと誤認しないよう `.md` 内で明確に分離 |
| パコラボ | **実データ**（第10期決算報告書） | 同社の了解済み |
| あつた鶏飯 | **実データ**（2026年5月実績） | 名古屋学院大学 杉浦礼子ゼミ |

いずれも `.md` の中で AI に対して次を指示している。

- 数値を勝手に変えたり丸めたりしない
- 資料に無い項目は推測で埋めず「記載がない」と明示する
- 成果物に出典と「演習用・社外配布不可」を明記する

**非公開リポジトリを公開に変えてはいけない。**

---

## 10. 残っている作業

| | |
|---|---|
| ⬜ | **`afdafbd`（部門AI対応）が未 push。** 回線復帰後に `git push` |
| ⬜ | `ROSTER` に2人目の学籍番号を追加（現在1名） |
| ⬜ | 学生が使い始める直前に `/api/admin/reset` でテスト分を消す |
| ⬜ | パコラボの第8期・第9期決算書が入手できたら3期化（配列に追加するだけ） |
| ⬜ | 必要ならパコラボにも `agents/pacolabo/*.md` を追加（6部門の定義あり） |

### 未検証の箇所

- **部門AIの実API動作**。構造・使用量・画面はスタブで確認済みだが、
  実際の Claude 応答は未確認
- Render 上での `PPTX_LIMIT` 到達時の挙動

---

## 11. このプロジェクトで守ってほしいこと

1. **費用に直結する変更は必ず実測してから判断する。** 推測で最適化すると外れる
   （`task_budget` を入れたがスライドの費用にはまったく効かなかった）
2. **push 前に `git ls-files` で中身を目視確認する。** 個人情報と実財務データを扱っている
3. **`disk_ok` を壊さない。** ここが false になると上限機能が全部無意味になる
4. **実在企業の数値を推測で補完しない。** 学生が実データと誤認する

---

## 12. 参考: 実測ログ

```
=== 対策前（Opus 5・履歴刈り込みなし）===
11:15:15  in=20,570  out=7,950   cache_w=278,875  cache_r=827,669  → 381.1円
11:17:43  in=299,022 out=1,596   cache_w=2,555    cache_r=316,617  → 264.9円
          ↑ 「さらにその」という一言に265円

=== 対策後（Sonnet 5 / effort=medium / 履歴刈り込みあり）===
11:02:31  セグメント分析＋Excel     →  17.9円
11:04:13  短い質問                 →   2.5円
11:06:58  スライド3枚              → 347.8円（履歴あり）
（履歴クリーン時のスライド単独     → 156.3円）
```
